# GP API Credentials
# Replace these sample values with your actual GP API credentials
# Get your credentials from: https://developer.globalpay.com/

# Backend GP API credentials (for server-side operations)
GP_API_APP_ID=your_gp_api_app_id_here
GP_API_APP_KEY=your_gp_api_app_key_here

# Optional: Environment (sandbox or production)
GP_API_ENVIRONMENT=sandbox
